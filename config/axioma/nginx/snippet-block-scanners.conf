# block-scanners.conf  (contexto server) - corta scanners marcados por $loggable.
# Se inserta al inicio de cada server{}; corre antes del 'return 301', asi
# tambien protege los vhosts de redireccion http->https. No toca trafico real.
if ($loggable = 0) { return 444; }
