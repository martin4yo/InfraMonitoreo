# stub_status para el colector de Netdata. Solo accesible desde localhost.
# Destino en el server: /etc/nginx/conf.d/netdata_stub_status.conf
server {
    listen 127.0.0.1:8088;
    server_name _;
    location = /stub_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
    }
}
